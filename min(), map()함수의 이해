n = int(input())
num = list(map(int, input().split()))  # 입력받는 값들을 정수꼴로 바꿔서 리스트에 저장해주는 문법이다.  map 을 쓴것이 특징.

result = num[0]
for i in num:
    result = min(i, result)  # min()이라는 함수는 두개의 파라미터를 받고 그중 작은 값을 리턴한다.
print(result)
